extends layouts/main-layout.pug

block content
    script.
        let postId = "!{postId}"
        let loggedInUser = !{loggedInUser}
    .postContainer
    +createReplyModal(loggedInUser)
    +createDeleteModal()
    +createPinPostModal()

block scripts
    script(src="/js/postPageScripts.js")